# Master Resume

Replace this file with your real master resume in Markdown. The artifact
generation agent reads this file plus `project_database.json` and tailors a
per-job resume by selecting and re-weighting content — it does not invent
experience that isn't present here.
